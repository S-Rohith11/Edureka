import React from "react";

const Dashboard = ({ user }) => {
  

      console.log(user)

    return (
        <div>
            {/* <h1>Welcome to the Dashboard, { user &&  user.displayName ? user.displayName : user.name}!</h1> */}
            {/* Add your dashboard content here */}
        </div>
    );
}

export default Dashboard;
